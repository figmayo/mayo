import { expect, it } from "vitest";
import { collection, variable } from "../build/lib";

it("should recognise the associated types in the built code", () => {
  const c = collection("tess", "Brand");
});
